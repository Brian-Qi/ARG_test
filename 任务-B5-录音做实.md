# 任务 B5 · 录音谜题「做实」

> 目标：把 `src/views/Audio.vue` 的录音谜题，从"看见字排序"改成**真正的听觉谜题**——关键信息藏在声音里。
> 本项目纯静态（Vue3 + Vite），**无后端**。

---

## ⚠️ 磁盘约定（重要）
**C 盘快满了，能放 F 盘的都别放 C 盘。** 新开的对话里请全程遵守：

| 用途 | 放哪 |
|---|---|
| 所有缓存 | `F:\cache`（或已有 `F:\ComfyUI` 作为 pip 缓存在用，可继续） |
| 工具（ffmpeg / venv / edge-tts） | `F:\tools\` |
| 生成中间文件 | `F:\ARG_test\_audio_tmp\` |
| 成品音频 | `F:\ARG_test\test_02\public\audio\` |

每次装东西前先设环境变量，例如：
```powershell
$env:PIP_CACHE_DIR="F:\cache\pip"
$env:TMP="F:\cache\tmp"; $env:TEMP="F:\cache\tmp"
# npm 缓存（如需）：
npm config set cache "F:\cache\npm" --global
```
**绝对不要**在 C 盘默认路径下 `pip install` / 下载 ffmpeg / 建 venv。

---

## 现状（要改的文件）
- `F:\ARG_test\test_02\src\views\Audio.vue`（约 74 行）
  - 现在：`clips = [{id:'信'},{id:'商'},{id:'义'},{id:'和'},{id:'让'}]`，把**德目字直接标在磁带上**，玩家只需排序；`bars` 是**随机假波形**；无真实音频。
  - `answer = ['义','让','信','和','商']`（五路次序：东→南→西→北→中）。
- 相关：`src/data/fragments.js` 的 `recording` 碎片说明；`src/stores/game.js` 的 `collectKey('huan-ming')`、`triggerScare`。

---

## 内容设计
5 盘磁带，**不标德目字**，只标 **甲/乙/丙/丁/戊**。每盘音频 = 孩子报数 + 埋在底噪下的**耳语德目字** + 算盘珠声：

| 磁带 | 藏的德目字（= 方位/财神） | 耳语样例 |
|---|---|---|
| 甲 | 义（东·比干） | "……让……让他……"（实藏"让"？→ 见下注） |
| … | … | … |

> 注：**具体哪盘藏哪个字由生成时决定**，用一段 5 条的映射写进 `Audio.vue` 即可。
> 五路次序固定为 **义 → 让 → 信 → 和 → 商**（东→南→西→北→中）。

**第六盘（己）**：男人的**算盘打断**——一播就 `game.triggerScare('face','不要数到六。')`，呼应主题。

---

## 步骤

### 1) 环境
```powershell
# 检查
ffmpeg -version          # 若无 → 下静态包解压到 F:\tools\ffmpeg\
# edge-tts（联网，微软神经音色）
$env:PIP_CACHE_DIR="F:\cache\pip"
pip install --target "F:\tools\pytools" edge-tts
```
- 若不想联网，退路：Windows 自带 SAPI（离线，机械音，够用）：
  ```powershell
  Add-Type -AssemblyName System.Speech
  $s = New-Object System.Speech.Synthesis.SpeechSynthesizer
  $s.SetOutputToWaveFile("F:\ARG_test\_audio_tmp\raw.wav")
  $s.Speak("一……让……"); $s.Dispose()
  ```

### 2) 生成 5–6 段音频
每段流程：`TTS 出干声 → 压低/加高通 → 混入底噪 + 算盘珠 + 报数 → 导出 mp3`。
```powershell
# 示例（逐段替换文本）
$env:PIP_CACHE_DIR="F:\cache\pip"
python -m edge_tts --voice zh-CN-YunxiNeural --text "二……商……商……" --write-media "F:\ARG_test\_audio_tmp\b.mp3"
ffmpeg -y -i "F:\ARG_test\_audio_tmp\b.mp3" -i "F:\ARG_test\_audio_tmp\noise.wav" -i "F:\ARG_test\_audio_tmp\suanpan.wav" `
  -filter_complex "[0]volume=0.55,highpass=f=300[a];[1]volume=0.22[b];[2]volume=0.4[c];[a][b][c]amix=inputs=3:duration=longest" `
  -t 8 -codec:a libmp3lame -q:a 4 "F:\ARG_test\test_02\public\audio\rec-a.mp3"
```
- 底噪/算盘珠可现场合成（`ffmpeg -f lavfi`）或用现成小素材；不必精细。
- 成品：`public/audio/rec-a.mp3 … rec-f.mp3`，每段 6–8s，总量目标 **< 1MB**。

### 3) 前端改造 `Audio.vue`
- `clips` 换成 `{ id:'甲', src:'/audio/rec-a.mp3', peaks:null }` 形式；**不再含德目字**。
- **真波形**：首次点播时 `fetch → decodeAudioData → 取峰值 → 渲染**；替换现在的随机 `bars`。
- 播放用 `AudioBufferSourceNode`（或隐藏 `<audio preload="none">`）。
- 答案映射：`answer` 按五路次序对应的**磁带 id 顺序**；内部用生成时定的 `id→字` 表判定。
- 第六盘 `己`：播完触发 `triggerScare`。
- 保留原有答对/答错文案与 `collectKey('huan-ming')`。

```js
// 取真波形（伪码）
const ctx = new (window.AudioContext||window.webkitAudioContext)()
async function load(clip){
  const ab = await (await fetch(clip.src)).arrayBuffer()
  const buf = await ctx.decodeAudioData(ab)
  const ch = buf.getChannelData(0), N=48, step=Math.floor(ch.length/N)
  clip.peaks = Array.from({length:N},(_,i)=>{ let m=0
    for(let j=i*step;j<(i+1)*step;j++) m=Math.max(m,Math.abs(ch[j]))
    return Math.max(6, Math.round(m*100)) })
}
```

### 4) 兜底（无障碍，必做）
听障/静音环境要有一条**非听觉**通路：
- 每盘旁给一枚「监听仪残迹」：一段乱码字幕，**德目字混在其中**（肉眼可辨）；或
- 在碎片网留一张**频谱图**（把字渲染成频谱纹理），可从《底档》间接推出。
> 让"听得到的人靠耳朵、听不到的人靠残迹"，两条路都通，符合本 ARG"硬而公平"的原则。

---

## 验收
- [ ] `/f/recording` 5 盘均可播、波形为**真实峰值**。
- [ ] 磁带**不显示德目字**；答对需"听出字 + 按五路次序"。
- [ ] 第六盘触发惊吓。
- [ ] 断网/静音时，兜底残迹仍能推出答案。
- [ ] 音频总量 <1MB，且**懒加载**（只在进该页时 fetch）。
- [ ] `npm run build` 通过；`C:\` 无新增文件。

---

## 备注
- edge-tts 依赖联网；若新对话环境无网，用 SAPI 离线方案（音色机械，但埋在噪声里足够）。
- 全部中间产物放 `F:\ARG_test\_audio_tmp\`，完成后可整体删除。
- 字体/图片等既有资产不用动。
